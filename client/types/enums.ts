export const enum ELoadStatus {
    ERROR = 'ERROR',
    IDLE = 'IDLE',
    RUNNING = 'RUNNING',
}

export const enum EErrorCodes {
    SERVER_ERROR = 500,
}

export const enum EMoveDirections {
    UP = 'UP',
    DOWN = 'DOWN',
}
